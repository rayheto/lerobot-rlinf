[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot-rlinf"
version = "0.0.1"
description = "SO-101 + Pi 0.5 post-training on Isaac Lab + RLinf"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
